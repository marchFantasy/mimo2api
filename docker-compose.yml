version: '3.8'

services:
  mimo-proxy:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./dbdata:/app/dbdata
    restart: unless-stopped
    environment:
      - NODE_ENV=production
